# Canonical origin used for metadataBase, robots.txt, and sitemap URLs.
# Leave unset in production — src/lib/site.ts already defaults to the live site.
# Set this on preview/staging deploys so crawlers and OG tags point at that host.
# NEXT_PUBLIC_SITE_URL=https://staging.example.org
