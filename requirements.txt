# Python dependencies for the data pipeline scripts (scripts/)
# Generated from a clean virtual environment — install with:
#   pip install -r requirements.txt
#
# Only process_species_images.py needs these packages.
# csv_to_json.py uses only Python's built-in libraries.

et-xmlfile==2.0.0
numpy==2.5.2
openpyxl==3.1.5
pandas==3.0.5
pillow==12.3.0
python-dateutil==2.9.0.post0
six==1.17.0
